# =========================
# App
# =========================
APP_ENV=development
PYTHON_PATH=.
ALLOWED_ORIGINS=["http://localhost:3000","http://127.0.0.1:3000"]

# =========================================================
# Database selection
#   Set DB_TYPE to either: postgresql  |  mssql
# =========================================================
DB_TYPE=postgresql

# =========================================================
# PostgreSQL (Docker Compose)
# - If you use the Postgres compose, keep THIS block active.
# - DB_HOST must match the compose service name: "postgres"
# - For local (no Docker), set DB_HOST=localhost and keep port 5432.
# =========================================================
DB_HOST=localhost
DB_PORT=5432
DB_USER=admin
DB_PASS="ChangeMe#2025"
DB_NAME=acredflow

BASEBALL_DATA_URL=
OPENAI_API_KEY=